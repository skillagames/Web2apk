rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global Safety Net (Relaxed for testing)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
